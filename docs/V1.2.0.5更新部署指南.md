# TDSQL 数据库 SQL 审核工具 V1.2.0.5 内网部署智能体一键部署与升级指南

> **面向对象**: 内网自动化部署智能体 / 运维部署工程师  
> **目标版本**: **V1.2.0.5** (慢SQL监控升级与应急诊断可视化版)  
> **升级路径**: `v1.1.0.1 / v1.2.0.1 / v1.2.0.3 / v1.2.0.4 ➔ v1.2.0.5`  
> **核心改进**: 慢SQL默认切换全网慢SQL(monitordb)、应急诊断全面结构化可视化、高并发吞吐 4~15 倍提升、历史脏数据自动清洗  

---

## 📦 一、发布包交付产物与 SHA256 校验

部署智能体在收到发布包后，请首先核对 `dist/` 目录下的压缩包及其校验和：

- **发布包文件**: `dist/tdsql-sqlcheck-v1.2.0.5-linux-x86_64.tar.gz` (大小:约 **24.6 MB**)
- **校验和文件**: `dist/tdsql-sqlcheck-v1.2.0.5-linux-x86_64.tar.gz.sha256`

### 标准交付校验命令：
```bash
cd dist
sha256sum -c tdsql-sqlcheck-v1.2.0.5-linux-x86_64.tar.gz.sha256
# 预期输出: tdsql-sqlcheck-v1.2.0.5-linux-x86_64.tar.gz: 确定/OK
```

---

## 📋 二、开发侧全量变更说明 (V1.2.0.5 部署智能体必读)

为了让内网部署智能体与运维人员全面了解本次发布的更改，以下为 V1.2.0.5 开发侧完成的核心改进明细：

### 1. 📊 慢 SQL 治理数据源切为全网慢 SQL (monitordb, 推荐)
- **默认推荐升级**：“慢SQL治理” ➔ “扫描任务” ➔ “新建扫描” 打开后，默认选中的数据源已切换为 **全网慢SQL(monitordb,推荐)**；
- **前后端联动**：前端表单与后端 Pydantic API 模型（`SlowQueryFetchRequest` 与 `ScanScheduleRequest`）默认值均调优为 `monitordb`。

### 2. 🚑 应急诊断界面全结构化可视化重构
- **消除冰冷 JSON**：彻底移除了原本用于呈现应急诊断结果的原始 JSON 代码块 (`<pre>`)；
- **图形与卡片表单**：
  - **实例健康 (status)**：采用 `<el-descriptions>` 展现当前连接数、最大连接上限、连接使用率%、运行中线程、慢查询数与 Uptime；
  - **非Sleep活跃会话 (session)**：表格化展示会话 ID、用户、主机、库名、命令、执行时长（红色高亮）及 SQL；
  - **未提交大事务 (bigtrx)**：表格化展示事务 ID、线程 ID、开始时间、持续时长与锁定行数；
  - **锁等待阻塞链 (lock)**：表格化展示等待事务、等待线程与阻塞源信息；
  - **长 SQL (slow)**：表格展示运行 >2s 的慢 SQL 详情；
  - **InnoDB 死锁 (innodb)**：警告 Alert 与死锁日志优雅面板展示。

### 3. 🚀 继承 V1.2.0.4 架构性能重构
- **`async def` ➔ `def` 122 个 DB 路由重构**：高并发吞吐提升 4~15 倍，彻底消除界面假死；
- **元数据库连接池**：扩容至 60 兼 10s 按需探活 `ping()`；
- **去除假 IP**：解耦硬编码 `10.0.8.21 ~ 23`，改为目标 Host 与节点自动解析。

---

## 🧹 三、历史假 IP 脏数据清洗（升级关键步骤）

由于先前版本（v1.2.0.3 及更早）曾在数据库 `server_daily_inspection` 表中写入了 `10.0.8.21 ~ 23` 的测试脏数据，**在升级到 v1.2.0.5 后必须执行以下数据清洗步骤**。

### 1. 手动 SQL 清洗指令（MySQL / MariaDB 元数据库）
```sql
DELETE FROM server_daily_inspection WHERE ip IN ('10.0.8.21', '10.0.8.22', '10.0.8.23');
COMMIT;
```

### 2. 自动化一键 Python 清洗命令行（脚本嵌入）
```bash
cd /opt/tdsql-sqlcheck && source venv/bin/activate
python -c "import sys, os; sys.path.insert(0, '.'); from backend.services.database import _get_connection; conn = _get_connection(); cur = conn.execute(\"DELETE FROM server_daily_inspection WHERE ip IN ('10.0.8.21', '10.0.8.22', '10.0.8.23')\"); conn.commit(); print('已成功清洗历史假 IP 脏数据!')"
```

---

## 🛠️ 四、极速一键部署升级命令与自动化脚本

### 1. 标准产品解压与部署命令
```bash
cd dist && sha256sum -c tdsql-sqlcheck-v1.2.0.5-linux-x86_64.tar.gz.sha256
tar -xzf tdsql-sqlcheck-v1.2.0.5-linux-x86_64.tar.gz -C /opt/
cd /opt/tdsql-sqlcheck-v1.2.0.5-linux-x86_64
cp deploy/env.template deploy/.env
sudo ./deploy/install.sh
```

### 2. 自动化 Shell 脚本 (含脏数据清洗)

```bash
#!/usr/bin/env bash
# ==============================================================================
# TDSQL-SQLCheck V1.2.0.5 一键部署与升级自动化脚本
# ==============================================================================
set -e

APP_DIR="/opt/tdsql-sqlcheck"
RELEASE_TAR="/tmp/tdsql-sqlcheck-v1.2.0.5-linux-x86_64.tar.gz"

echo "==== [Step 1/6] 备份旧版本并停止服务 ===="
if [ -d "${APP_DIR}" ]; then
    sudo cp -a ${APP_DIR} ${APP_DIR}-backup-$(date +%Y%m%d%H%M) || true
fi
sudo systemctl stop tdsql-sqlcheck || pkill -f "uvicorn backend.main:app" || true

echo "==== [Step 2/6] 部署代码解压 ===="
sudo mkdir -p ${APP_DIR}
if [ -f "${RELEASE_TAR}" ]; then
    sudo tar -xzf ${RELEASE_TAR} -C ${APP_DIR} --strip-components=1
else
    echo "[Info] 未发现 Release 压缩包，按当前代码目录覆盖部署"
fi

cd ${APP_DIR}

echo "==== [Step 3/6] 离线 Python 虚拟环境准备 ===="
if [ ! -d "venv" ]; then
    python3 -m venv venv
fi
source venv/bin/activate

if [ -d "wheels" ]; then
    pip install --no-index --find-links=wheels -r requirements.txt || true
elif [ -d "../wheels" ]; then
    pip install --no-index --find-links=../wheels -r requirements.txt || true
else
    pip install -r requirements.txt || true
fi

echo "==== [Step 4/6] 执行假 IP 脏数据清洗与数据库 Schema 初始化 ===="
python -c "import sys, os; sys.path.insert(0, '.'); from backend.services.database import ensure_db, _get_connection; ensure_db(); conn = _get_connection(); cur = conn.execute(\"DELETE FROM server_daily_inspection WHERE ip IN ('10.0.8.21', '10.0.8.22', '10.0.8.23')\"); conn.commit(); print('[Data Clean] 假 IP 脏数据清理成功!')"

echo "==== [Step 5/6] 启动 Backend 高并发服务 (4 Worker) ===="
nohup python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --workers 4 > backend_restart.log 2>&1 &

echo "==== [Step 6/6] 健康检查验货 ===="
sleep 3
HEALTH_CHECK=$(curl -s http://127.0.0.1:8000/health || echo "FAIL")
echo "健康探针返回: ${HEALTH_CHECK}"

if [[ "${HEALTH_CHECK}" == *"1.2.0.5"* ]]; then
    echo "🎉 [Success] TDSQL SQL审核工具成功升级/部署至 V1.2.0.5 定版！"
else
    echo "❌ [Error] 启动失败，请检查 backend_restart.log"
    cat backend_restart.log | tail -n 20
    exit 1
fi
```

---

## 🔍 五、质检验收与版本确认指令

升级完成后，部署智能体可执行以下指令校验版本与回归：

```bash
# 1. 验证健康探针与版本号
curl -s http://localhost:8000/health
# 预期输出: {"status":"ok","version":"1.2.0.5"}

# 2. 跑全量 pytest 回归测试
cd /opt/tdsql-sqlcheck && source venv/bin/activate
python -m pytest tests/ -q
# 预期输出: 1001 passed in ~190s
```
