# v1.5.2.4 准出质检结论（智能体 A）

| 项 | 内容 |
|---|---|
| 被测版本 | 初检 `main @ 4272f0a`；**本文结论对应 `main @ <本次提交>`**（含 A 的三项收口 `9a2336f` 与 O 复核后的两项回归修复） |
| 输入 | Q 的修复批（`ce493de`…`5fba7ad`）、O 的复测报告、G 的真实 TDSQL 实测报告 |
| 质检人 | 智能体 A（独立复核，未采信任何一方的自述结论） |
| 日期 | 2026-07-31 |
| **结论** | 🟢 **准出**——三项均已由 A 直接修复并验证，可打包发布 |
| 修订 | 第 2 版：§3.1 / §3.3 已修复，§3.4 已补记（回归 1109 通过）<br>**第 3 版：O 复核发现我引入/遗漏的 2 项 P1，已修复，见 §6；回归 1117 通过 / 0 失败** |

---

## 1. 拍板

**准出。可以让 G 打包发布。**

Q 的修复批我逐项独立复验通过；本轮质检新发现的三项，已按项目负责人指示**由 A 直接修复**：

| 新发现项 | 处置 | 验证 |
|---|---|---|
| §3.1 打包脚本版本号硬编码 1.4.0.1 | ✅ 已改为从 `VERSION` 文件读取，并补 `--version` / `--py` 参数 | 参数解析四种组合实测 |
| §3.3 R043 DELETE 分支误报 | ✅ 已收敛到目标段，并补 7 项回归 | 19 例矩阵全过；变异实测确认新用例真能失败 |
| §3.4 G 报告覆盖声明不实 | ✅ 已补记未执行 5 例 + 残留风险评估，表头与结论同步改写 | —— |

**全量回归 1117 通过 / 0 失败 / 0 错误**（初检 1102 → 三项收口 1109 → O 复核修复 1117）。

> **第 3 版更正**：第 2 版我曾写"准出，三项均已修复并验证"。O 随后复核发现我的修复本身
> 带了 2 项 P1（一项遗漏、一项由我新引入），说明我第 2 版的验证不够。两项现已修复并
> 补回归，详见 §6。**结论仍为准出，但这个更正必须留在文档里。**

| 项 | 判定 |
|---|---|
| P0-01 / P1-02 / P1-02b / P2-03 / P2-04 / P1-05 | ✅ 全部修复，独立复验通过 |
| 全量回归 | ✅ 1102 通过 / 0 失败 / 0 错误 |
| 升级路径 1.2.0.9 → 1.5.2.4 | ✅ 与全新安装逐列一致，未因调序回归 |
| R-17 护栏 | ✅ 变异实测有效 |
| **打包脚本版本号** | 🔴 **必改，否则部署验证必失败** |
| R043 DELETE 分支残留误报 | 🟠 建议同批修（相对内网现版本是**新增**误报） |
| G 实测的覆盖声明 | 🟠 计划 15 例只做了 12 例，报告未披露 |

---

## 2. 我做了什么复验（不采信自述）

### 2.1 逐项复验

| 缺陷 | 我的验证手段 | 结果 |
|---|---|---|
| **P0-01** | 仓库原样 `import backend.main`；真起 uvicorn | ✅ 导入 OK，34 条路由；`已就绪 (V1.5.2.4)` |
| **P1-02** | 全新库**第一次**启动查 6 列；再走登录→改密→新口令登录→旧令牌鉴权 | ✅ 首启即 **6/6**（修复前 0/6）；改密 200、新口令登录 200、**旧令牌 401**（会话吊销真生效） |
| **P1-02 回归** | 重跑「1.2.0.9 建库 → 1.5.2.4 升级 → 与全新安装逐列逐索引比对」 | ✅ 缺表/缺列/多列/列定义/缺索引**全无差异**，升级库 6/6；连启两次结构不变 |
| **P2-03** | **自建 12 例矩阵**（6 正向 + 6 反向鉴别），不用他们的用例 | ⚠️ 11/12，1 例残留误报，见 §3.1 |
| **P2-04** | 自建截断文件（t1 完整 / t2 截断 / t3 完整）+ 正常文件对照 | ✅ 截断文件切出 3 条；正常文件切出 2 条**未过切** |
| **P1-05** | 对全工作区做 SHA256 指纹扫描 | ✅ 未再检出活口令明文；守卫用例通过；凭据改环境变量 |

### 2.2 护栏有效性（变异实测）

R-17 只写在文档里不算数。我在沙箱里**重演了 a1fdc53 的原始事故**（删掉 `StatusUpdateRequest` 类定义）：

| 护栏 | 是否拦住 |
|---|---|
| `tests/test_app_routes_integrity.py` | ✅ 收集阶段即报 NameError |
| `deploy/preflight_check.sh` 第 7 节 | ✅ `import backend.main` 失败 → `bad` → 禁止部署 |

**护栏真能挡住它本该挡的事故。**

### 2.3 前端

✅ 标题 `V1.5.2.4`、登录页正常渲染、**无 CSP 违规、无 JS 报错**；L-04 时间窗提示的 18 项回归全绿。

### 2.4 关于 O 报告里的 P1-NEW-01（docker-compose 元库注入）

**核实后不影响本次内网发布。** `deploy/make_release.sh` 只打包 `backend/ frontend/ requirements.txt deploy/*.sh|*.service|env.template|nginx.conf|README.md docs/*.md wheels/`，**`docker-compose.yml` 根本不进发布包**。该缺陷只影响本地 compose 起服务的开发链路，与内网 systemd 部署路径无关。O 的判断本身没错，但对本次准出不构成阻断。

---

## 3. 本轮新发现

### 3.1 🔴 必改：`make_release.sh` 版本号硬编码 1.4.0.1，会让部署验证确定性失败

**证据链**：

```
deploy/make_release.sh:12   VERSION="1.4.0.1"        ← 硬编码，且参数解析里没有 --version
      ↓ 产出 tdsql-sqlcheck-v1.4.0.1-linux-x86_64.tar.gz，包内 VERSION 文件写 1.4.0.1
deploy/verify_deploy.sh:13  EXPECTED_VER=$(cat ../VERSION)        → 1.4.0.1
deploy/verify_deploy.sh:20  VER=$(curl /health | ...)             → 1.5.2.4
deploy/verify_deploy.sh:21  [[ "$VER" == "$EXPECTED_VER" ]]       → 假
                            bad "版本号异常: 1.5.2.4(期望 1.4.0.1)"
deploy/verify_deploy.sh:68  FAILC>0 → exit 1                      ← 部署验证整体失败
```

仓库根 `VERSION` 文件是对的（`1.5.2.4`），`backend/config.py:25` 也是 `1.5.2.4`——**只有打包脚本没跟上**，而且它会用自己的硬编码值**覆盖**包内的 VERSION 文件。

**修复方法**（二选一，推荐后者）：

```bash
# 简单：改这一行
VERSION="1.5.2.4"

# 更好：从仓库 VERSION 文件读，永不再漂移
VERSION="$(cat "$(dirname "${BASH_SOURCE[0]}")/../VERSION" | tr -d '\r\n')"
```

**验收**：打出的包名含 `v1.5.2.4`，包内 `VERSION` 为 `1.5.2.4`，目标机 `verify_deploy.sh` 版本号一项 PASS。

### 3.2 🟠 附带：`--py` 参数文档里有、实现里没有

`make_release.sh` 的用法注释写着 `--arch aarch64 --py 39`，但参数解析只有 `--arch` / `--with-python`，其余走 `*) shift`。实测 `--arch aarch64 --py 39` 得到的是 `PYTAG=311`。

**后果**：目标机若是 python3.9（`preflight_check.sh` 明确支持 3.9），按文档加 `--py 39` 打包，实际下载的仍是 **cp311 wheels**，装到目标机上会失败，而打包时无任何提示。

**修复方法**：给解析器补一条 `--py) PYTAG="$2"; shift 2;;`；或删掉用法注释里的 `--py`，二者必居其一，不要让文档和实现对不上。

### 3.3 🟠 R043 的 DELETE 分支仍有误报，且相对内网现版本是**新增**的

Q 把 UPDATE 分支限定到了 `UPDATE..SET` 目标段，修得对；但 **DELETE 分支原样未动**：

```python
backend/engine/parser/parser_legacy.py:261
re.search(r"\bdelete\s+.*?\bjoin\b", clean_sql_no_comm, re.DOTALL)
```

`.*?\bjoin\b` 不限范围，**WHERE 子查询里的 JOIN 也会被算成联表 DELETE**：

| SQL | 判定 |
|---|---|
| `DELETE FROM t_a WHERE id IN (SELECT id FROM t_b JOIN t_c ON t_b.k=t_c.k)` | ❌ **误报 R043** |
| `DELETE FROM t_a WHERE id IN (SELECT id FROM t_b, t_c WHERE t_b.k=t_c.k)` | ✅ 不报（逗号式反而没事） |
| `DELETE a FROM t_order a JOIN t_user b ON a.cid=b.id` | ✅ 正确命中 |

**关键点：内网现行 1.2.0.9 对这条 SQL 不报警**（JOIN 检测是 `7c90fa5` 之后才加的）。也就是说**升级会引入一个原来没有的误报**。

**影响**：R043 是 ERROR 级，而 v1.4 质量门禁的 ERROR 阈值默认为 0——一条合法的 `DELETE ... WHERE id IN (SELECT ... JOIN ...)` 会被**挡在门禁外**，属于上线即可能被投诉的类型。

**修复方法**（与 UPDATE 分支同构，取 DELETE 与 FROM/WHERE 之间的目标段）：

```python
# DELETE：只看 FROM 之前的目标段与 FROM 后的直接表引用，
# 不能用 `.*?\bjoin\b` 满句扫 —— WHERE 子查询里的 JOIN 会被误算成联表 DELETE。
m_del = re.search(r"\bdelete\b(.*?)\bwhere\b|\bdelete\b(.*)$",
                  clean_sql_no_comm, re.DOTALL)
del_seg = (m_del.group(1) or m_del.group(2) or "") if m_del else ""
del_multi = bool(re.search(r"\bjoin\b", del_seg)
                 or re.search(r"\bdelete\s+[a-zA-Z0-9_`\s,]+from\b", clean_sql_no_comm))
```

**必须补的反向用例**（R-12）：`DELETE FROM t_a WHERE id IN (SELECT id FROM t_b JOIN t_c ON t_b.k=t_c.k)` 不得命中。

> 严重度说明：这是**误报**方向，按 R-15 属"看得见的错"，比修好的那个静默漏报轻一个量级，也可以靠规则集临时关掉 R043 兜底。所以我不把它列为准出阻断，但**强烈建议同批修**——一行的事，且它会在上线第一天就打扰使用者。

### 3.4 🟠 G 的实测覆盖与报告声明不符

G 自己的方案 `TEST-v1.5.2.4-真实TDSQL专项实测方案-G.md` 定义了 15 个用例（T1.1–T4.3），结果报告执行了 12 个，其中 2 个（T5.2 / T5.5）还不在方案里。**方案里有、报告里没做且未说明的有 5 个**：

| 未执行 | 内容 | 为什么值得在意 |
|---|---|---|
| **T2.3** | ZK 与探测交叉，**保守合并实证** | v1.5.1 的核心安全属性（R-15 保守方向），没实证 |
| **T4.3** | 声明被探测纠偏 | 正是引出整个 v1.5 的那类缺陷（声明说了不算） |
| **T3.2** | **时间窗过滤** | 我在上一轮报告里点名要求覆盖的项 |
| T2.2 | ZK 注册 + 形态落库 | 次要 |
| T3.3 | digest / processlist 对照 | 另两个数据源完全没在真机上跑过 |

而报告结论写的是「**全部关键专项实测用例通过（PASS）**」——**没有披露这 5 项没做**。

**客观地说**：最要害的 T4.1 / T4.2（集中式跳过 27 条规则不触发 R077、分布式触发 R077）**确实通过了**，这是使用者能直接感知的核心行为；T1.4 锁定优先级也过了，判定链走通了一部分。所以我不认为覆盖缺口大到要卡准出。

**但报告不能这么写。** 请 G 要么补跑这 5 例，要么在报告里如实标注"未执行 + 原因 + 残留风险"，由你签字接受。**测试报告谎报覆盖率，比少测几个用例危险得多。**

### 3.5 🔵 轻微：压测配置里管理员口令仍留硬编码默认值

```python
tests/pressure_test/config.py:14
API_PASSWORD = os.getenv("SQLCHECK_PT_API_PASSWORD", "Admin@1234")
```

两个实例口令改得很干净（`os.getenv(...)` 无默认值，取不到即为 None），唯独管理员口令留了 `"Admin@1234"` 兜底。建议与另外两个拉齐，去掉默认值。

---

## 4. 准出条件

**打包前必做**：✅ 已全部完成（§3.1 / §3.3 / §3.4，见 §1）。

**顺带一并做了**：§3.2 `--py` 参数缺失——已补进参数解析，并对非法值直接报错退出，不再静默忽略。

**可以放到下个版本（1 项）**：§3.5 压测配置里管理员口令的硬编码默认值。

**结论：可以打包发布。** 建议 G 打包后照常跑一次 `deploy/verify_deploy.sh`，版本号一项现在应为 PASS。

---

## 5. 测试环境口径（如实声明）

| 项 | 实际情况 |
|---|---|
| 元数据库 | MariaDB 10.11（容器内自建），生产为 MySQL，**并非完全等价** |
| 真实 TDSQL | 本容器出网策略不放行，**我本轮仍未实测**；该部分采信 G 的报告，但已指出其覆盖声明的问题（§3.4） |
| 全量套件 | 1102 通过 / 0 失败 / 83 跳过 / 0 错误（跳过项为无 docker-compose 种子库与非 Windows 环境） |
| 复验方式 | 全部用**我自己构造的用例**，未直接采信 Q/O/G 的用例与结论 |


---

## 6. 第 3 版：O 复核发现的两项 P1（已修复）

O 对第 2 版做了独立复核，提出两项 P1 回归。**两项经实测均成立，我的修复确有疏漏。**

### 6.1 R043 仍误报带修饰词的合法单表 DELETE（我漏了）

**O 的判断正确。** 实测四种形态全部误报：

| SQL | 修复前 | 修复后 |
|---|---|---|
| `DELETE LOW_PRIORITY FROM t_a WHERE id=1` | ❌ 误报 | ✅ |
| `DELETE QUICK FROM t_a WHERE id=1` | ❌ 误报 | ✅ |
| `DELETE IGNORE FROM t_a WHERE id=1` | ❌ 误报 | ✅ |
| `DELETE LOW_PRIORITY QUICK IGNORE FROM t_a WHERE id=1` | ❌ 误报 | ✅ |

**根因**：`DELETE LOW_PRIORITY FROM t` 的目标段是 ` low_priority from t`，与 `DELETE a FROM t`
在形态上**完全一致**，被"别名列表"正则判成联表删除。

**为什么我没发现**：我的 19 例矩阵覆盖了子查询、反引号、大小写、LIMIT，
**唯独没有 MySQL 的 DML 修饰词**。这类"合法但不常写"的语法正是误报的高发区。

**修复**：判定前先剥掉段首连续出现的修饰词，只剥段首、不碰后面的表名
（表名可以就叫 `low_priority`，已加用例守住）。

```python
del_seg = re.sub(r"^\s*(?:(?:low_priority|quick|ignore)\s+)+", " ", del_seg)
```

**回归**：新增 8 项（4 反向修饰词 + 3 正向"修饰词+真联表"防剥过头 + 1 表名同名）。
变异实测：撤掉剥词后**恰好 4 条反向用例失败**。

### 6.2 `--py 39 --with-python` 会产出装不上的包（**我引入的**）

**O 的判断正确，且这是我第 2 版新引入的回归。**

修复 `--py` 之前它被 `*) shift` 吞掉、恒为 311，与内置的 CPython 3.11 **恰好一致**；
我把 `--py` 改成真的生效之后，`--py 39 --with-python` 就会下 cp39 的 wheels、却内置
3.11 运行时。目标机无系统 Python 时用的正是内置解释器，离线装依赖必因 ABI 不匹配失败，
**而打包阶段毫无提示**。

**修复**：`--with-python` 时目标机跑的就是内置解释器，wheels 必须按它的 ABI 走。
二者显式冲突时**直接拒绝，不做静默纠正**：

```
[--py 39 --with-python]  → 拒绝: --with-python(内置 cp311) 与 --py 39 冲突
[--py 311 --with-python] → 接受
[--arch aarch64 --py 39] → 接受（不内置 Python，目标机自备 python3.9）
```

**顺带修**：该脚本没有 `set -e`，便携 Python 的 `curl`/`tar` 失败不会中断，会打出一个
"声称内置 Python 但实际没有"的包。已对下载、解包、可执行性三步各加显式拦截。

### 6.3 O 提的另外两项非阻断意见

| 意见 | 处置 |
|---|---|
| 本文表头版本号与实际修复提交对不上 | ✅ 已改。初检基线与结论对应提交分别标注 |
| G 报告的 5 项真机覆盖缺口仍在 | ✅ 认同。补记只解决"如实披露"，不等于补上覆盖。缺口与残留风险见 G 报告 §3.2，仍需你决定是补跑还是接受 |

### 6.4 这轮暴露的问题

两次都栽在**字符组/分支的边界没想清楚**：`[^set]`（原缺陷）→ `[\w\s,]+` 前导空格
（我第一版）→ 修饰词被当别名（我第二版）。同一处判据连错三次。

**给团队的建议**：正则型判据的反向用例，不能只挑"常见的合法写法"，
必须把该语句的**完整合法语法**过一遍（MySQL DELETE 的 `LOW_PRIORITY/QUICK/IGNORE`、
UPDATE 的 `LOW_PRIORITY/IGNORE`、`PARTITION` 子句等）。
更稳的做法是改用 AST 判定而非正则——本项目已依赖 sqlglot，**建议下个版本把 R043
迁到 AST**，这类边界问题会整类消失。
