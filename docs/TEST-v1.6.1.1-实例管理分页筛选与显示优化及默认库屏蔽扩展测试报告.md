# TEST-v1.6.1.1 实例管理分页筛选与显示优化、默认库屏蔽扩展 测试报告

> 被测版本：v1.6.1.1　依据：`DESIGN-v1.6.1.1`、`DEV-v1.6.1.1`
> 结论：🟢 四项反馈均按设计落地，全量回归 0 失败 0 跳过，可交智能体 A 复测

## 一、新增用例（tests/test_zk_v1611.py，3/3 通过）

| 用例 | 覆盖 | 结果 |
|---|---|---|
| 富集路径排除 query_rewrite / xa / sysdb | 变更三富集侧 | ✅ |
| 预检路径排除 query_rewrite / xa | 变更三预检侧 | ✅ |
| 前端结构守卫：分页绑定+el-pagination、三维筛选控件、地址 260/操作 480/WARNING 120、td-nowrap/th-nowrap CSS 齐备 | 变更一/二展示层 | ✅ |

## 二、既有用例适配

- `test_zk_discovery.py`：app.js 缓存参数断言同步 `?v=20260806.3`；
- v1608/v1605 的 sysdb 用例继续有效（排除集为超集关系）。

## 三、回归

| 口径 | 结果 |
|---|---|
| 全量 `pytest tests/` | **1286 通过 / 0 失败 / 0 跳过**（较 v1.6.1.0 基线 1283 +3 新用例） |
| `node --check frontend/static/js/app.js` | 通过 |

## 四、给 A 的复测建议

1. 实例管理页：造 >20 条连接验证分页（翻页、改页大小、total 计数）；
   筛选三条件 AND、清空回第一页；连接名模糊不区分大小写；
2. 换行：地址列（最长 `host:port/database`）与操作列（admin 全按钮形态）单行完整显示，
   WARNING 表头单行；窄窗口下横向滚动且操作列右固定；
3. ZK 导入预览：含 query_rewrite / xa 的实例不再生成二者候选行；扫描列表"N 个库"计数同步不含；
4. 反向鉴别：手工业务库填 xa 仍保留（显式意图）。

## 五、未覆盖声明

- 分页/筛选/换行为纯前端行为，结构守卫已覆盖关键绑定与样式；最终观感需浏览器确认
  （建议内网或 A 的浏览器环境目视一遍操作列单行效果）。
