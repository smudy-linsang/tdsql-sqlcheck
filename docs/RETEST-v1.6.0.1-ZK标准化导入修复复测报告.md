# RETEST-v1.6.0.1 ZK 标准化导入修复复测报告

> 复测对象：`FIX-v1.6.0.1-ZK标准化导入问题修复说明书.md` 所列 P1~P8 修复（commits `15fdcf8` / `f1b9e90` / `dd6ca73`）
> 复测环境：Windows 开发机，Python 3.14，FastAPI 0.139，MySQL 8.0（127.0.0.1:13306），真开 Chromium
> 日期：2026-08-03
> **复测结论：🟢 8 项问题全部修复确认有效；全量回归 1220 通过 / 0 失败；修复未引入新缺陷**

---

## 1. 逐项复测结果

| 编号 | 修复内容 | 复测方法 | 结果 |
|---|---|---|---|
| P1 | G 报告结论降级加注 | 查阅 `REPORT-...-G.md` 文件头加注与设计 §13 回写 | ✅ 加注到位，原文保留可追溯 |
| P2 | 提交路径 9 用例入正式套件 | `pytest tests/test_zk_import_commit.py` | ✅ **9/9 通过** |
| P3 | 错误码契约回写 | 核对设计 §6.3/§12 与代码注释 | ✅ `BUSINESS_PROXY_INCOMPLETE` 已入契约，从严口径固化 |
| P4 | 失败批次留痕 + candidate_count | 定向用例断言（冲突后 `status='failed'` 批次存在、`failure_summary` 含错误码不含口令；2 预览/1 选中 → candidate_count=2） | ✅ 通过 |
| P5 | Mock 醒目标识 | 浏览器 UAT（FORCE_MOCK 服务，8012 端口） | ✅ 红色警示条 + 每行"演示"标签渲染正确（截图 `docs/RETEST-v1601-mock-alert.png`）；Mock 导入拦截回归正常 |
| P6 | 占位地址防回写 | 浏览器打开 ZK 配置弹窗（FORCE_MOCK 下） | ✅ "ZK 服务地址"为空，`mock.invalid:2181` 不再回显 |
| P7 | 导入结果批次明细 | 代码走查 + API 响应契约核对（`connections` 数组已在单测中验证返回） | ✅ 前端改为批次明细弹窗；**完整浏览器闭环需真实 ZK 导入，归入内网 UAT** |
| P8 | 多副本会话约束 | 查阅 `V1.6.0.1全量更新部署说明.md` 新增章节 | ✅ 约束成文（会话粘滞或共享存储二选一） |

## 2. 回归测试

| 项 | 修复前基线 | 复测结果 | 判定 |
|---|---|---|---|
| 全量套件 `pytest tests` | 1212 通过 / 0 失败 / 28 跳过 | **1220 通过 / 0 失败 / 29 跳过** | ✅ 零回归，净增 8 条守护用例 |
| ZK 专项（`test_zk_discovery.py` + `test_zk_import_commit.py`） | 18 通过 | **27 通过** | ✅ |
| 前端语法 `node --check app.js` | 通过 | 通过 | ✅ |
| 后端语法 `compileall` | 通过 | 通过 | ✅ |

**说明**：复测首轮曾出现 1 条失败（`test_zk_config_frontend_exposes_admin_entry_and_redacted_password_flow`），
原因是该用例是前端缓存版本守卫，断言固定了 `app.js?v=20260803.3`，而 P5/P7 修复按惯例 bump 至 `.4`。
已按守卫本意同步版本号，并把 P5 的"演示标识"三要素（警示条/标签/状态暴露）追加为守护断言——
**守卫机制按设计发挥了拦截作用，属良性失败**（commit `dd6ca73`）。

## 3. 浏览器 UAT 复测记录（真启服务，人类用户视角）

服务：全新空库 `tdsql_retest_v1601` + `ZK_DISCOVERY_FORCE_MOCK=1`，端口 8012。

| # | 场景 | 结果 |
|---|---|---|
| R1 | 登录页渲染 V1.6.0.1、admin 登录成功（旧库令牌自然失效，重新登录） | PASS |
| R2 | 实例管理 → ZK 配置弹窗：服务器地址为空（P6）、口令掩码、无回显 | PASS |
| R3 | 自动发现 → 开始扫描：返回 3 条 mock 实例，**红色警示条**"当前为演示（Mock）数据…禁止导入"出现（P5） | PASS |
| R4 | 列表每行状态列出现红色"**演示**"标签（P5） | PASS |
| R5 | 选中 mock 实例点"配置导入并生成预览"→ 前端拦截"Mock 发现结果禁止导入"（回归确认） | PASS |
| R6 | JS 控制台：除一次预期的旧令牌 401 外无错误、无 CSP 违规 | PASS |

临时库 `tdsql_retest_v1601` 复测后已删除，无残留。

## 4. 遗留事项（与修复无关，维持原判）

1. **内网真实集群 UAT 仍是投产前置**：kazoo 真连发现、MonitorDB 名称解析、多 Proxy
   `SHOW DATABASES` 一致性、P7 批次明细弹窗的完整浏览器闭环——本地环境无法替代，
   按 O 自测报告 §4 五条清单执行。
2. 部署若采用多副本形态，先落实部署说明第三章 §4 的会话粘滞/共享存储方案。

## 5. 结论

P1~P8 全部修复并经复测确认有效；全量回归零失败；修复批未引入新缺陷。
**当前状态：代码与测试达到投产质量门槛，放行条件仅剩内网真实集群 UAT 闭环一项。**

*复测全程未再修改产品代码（除守卫用例按版本 bump 同步）；所有临时资源已清理。*
