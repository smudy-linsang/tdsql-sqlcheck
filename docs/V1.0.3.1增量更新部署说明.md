# TDSQL数据库SQL审核工具 v1.0.3.1 增量更新部署说明

> **版本**: v1.0.3 → v1.0.3.1
> **发布日期**: 2026-07-10
> **更新类型**: 增量更新（Bug修复，无破坏性变更）
> **前置条件**: 已按《部署手册-v1.0.2》完成 v1.0.2 初始部署，并已升级至 v1.0.3

---

## 一、本次更新内容（v1.0.3 → v1.0.3.1）

### 修复项（3项）

| 编号 | 模块 | 问题描述 | 修复内容 |
|------|------|---------|---------|
| BUG-1 | 大表治理 | 采集大表清单后页面不显示数据 | 修复字段名映射（`schema`/`table`/`rows` 而非 `schema_name`/`table_name`/`rows_count`） |
| BUG-2 | 部署脚本 | verify_deploy.sh 版本号断言仍为 1.0.3 | 更新为 1.0.3.1 |
| BUG-3 | 版本标识 | 多处版本号仍为 1.0.3 | 统一升级为 1.0.3.1 |

### 无变更项

- 数据库结构：无变更
- 配置文件（.env）：无变更，v1.0.3 的 .env 完全兼容
- Python依赖：无新增（仍为 38 个 wheels）
- API接口：无变更

---

## 二、发布包信息

| 项目 | 值 |
|------|-----|
| 文件名 | `tdsql-sqlcheck-v1.0.3.1-linux-x86_64.tar.gz` |
| 文件大小 | **24.4 MB** |
| SHA256 | `35db37fac2c5e9a6285fa4a809e98ae43acea2fbe3a6b23dd6d8154c38df2d62` |
| Wheels | 38 个（含 httpx + python-multipart） |
| 目标架构 | Linux x86_64 |

---

## 三、部署步骤（严格按顺序执行）

### 第1步：传输发布包到目标机

将 `tdsql-sqlcheck-v1.0.3.1-linux-x86_64.tar.gz` 和 `tdsql-sqlcheck-v1.0.3.1-linux-x86_64.tar.gz.sha256` 传输到目标机 `/tmp/` 目录。

传输方式（三选一）：
- U盘拷贝
- SCP: `scp tdsql-sqlcheck-v1.0.3.1-linux-x86_64.tar.gz* user@目标机IP:/tmp/`
- 内网文件共享

### 第2步：SHA256校验

```bash
cd /tmp
sha256sum -c tdsql-sqlcheck-v1.0.3.1-linux-x86_64.tar.gz.sha256
```

**期望输出**: `tdsql-sqlcheck-v1.0.3.1-linux-x86_64.tar.gz: OK`

如果不是OK，**停止操作**，报告校验失败。

### 第3步：解压

```bash
tar -xzf tdsql-sqlcheck-v1.0.3.1-linux-x86_64.tar.gz
cd tdsql-sqlcheck-v1.0.3.1-linux-x86_64
ls -la
```

确认目录内有 `backend/`、`frontend/`、`deploy/`、`wheels/`、`requirements.txt`、`VERSION` 等文件。

### 第4步：停止当前服务

```bash
sudo systemctl stop tdsql-sqlcheck
sudo systemctl status tdsql-sqlcheck
```

确认服务已停止（Active: inactive）。

### 第5步：备份当前版本

```bash
sudo cp -a /opt/tdsql-sqlcheck /opt/tdsql-sqlcheck-v1.0.3-backup
ls -la /opt/tdsql-sqlcheck-v1.0.3-backup/
```

确认备份目录存在且内容完整。

### 第6步：覆盖更新代码（保留 .env 和 data/）

```bash
# 覆盖后端代码
sudo cp -r /tmp/tdsql-sqlcheck-v1.0.3.1-linux-x86_64/backend/ /opt/tdsql-sqlcheck/

# 覆盖前端代码
sudo cp -r /tmp/tdsql-sqlcheck-v1.0.3.1-linux-x86_64/frontend/ /opt/tdsql-sqlcheck/

# 覆盖requirements.txt
sudo cp /tmp/tdsql-sqlcheck-v1.0.3.1-linux-x86_64/requirements.txt /opt/tdsql-sqlcheck/

# 覆盖VERSION文件
sudo cp /tmp/tdsql-sqlcheck-v1.0.3.1-linux-x86_64/VERSION /opt/tdsql-sqlcheck/

# 覆盖部署脚本（可选，供后续运维使用）
sudo cp -r /tmp/tdsql-sqlcheck-v1.0.3.1-linux-x86_64/deploy/ /opt/tdsql-sqlcheck/
```

**重要**：不要覆盖 `.env` 文件和 `data/` 目录，保留原有配置和数据。

### 第7步：安装/更新Python依赖（离线）

```bash
cd /opt/tdsql-sqlcheck
sudo /opt/tdsql-sqlcheck/venv/bin/pip install --no-index --find-links=/tmp/tdsql-sqlcheck-v1.0.3.1-linux-x86_64/wheels/ -r requirements.txt
```

**期望输出**: 所有依赖 `Successfully installed` 或 `already satisfied`。

### 第8步：启动服务

```bash
sudo systemctl start tdsql-sqlcheck
sleep 5
sudo systemctl status tdsql-sqlcheck
```

确认服务 Active: active (running)。

### 第9步：执行部署验证脚本

```bash
cd /opt/tdsql-sqlcheck
sudo bash deploy/verify_deploy.sh
```

**期望输出**: 全部 PASS，0 FAIL。

或手动验证：

```bash
# 健康检查（版本号应为1.0.3.1）
curl -s http://127.0.0.1:8000/health | python3 -m json.tool

# 前端可访问
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/

# 静态资源
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/static/js/app.js
```

---

## 四、回滚方案（如升级失败）

```bash
sudo systemctl stop tdsql-sqlcheck
sudo rm -rf /opt/tdsql-sqlcheck
sudo cp -a /opt/tdsql-sqlcheck-v1.0.3-backup /opt/tdsql-sqlcheck
sudo systemctl start tdsql-sqlcheck
```

---

## 五、验证清单（部署后逐项确认）

| 序号 | 验证项 | 期望结果 | 实际结果 |
|------|--------|---------|---------|
| 1 | `curl /health` 版本号 | `"version":"1.0.3.1"` | |
| 2 | 前端页面可访问 | HTTP 200 | |
| 3 | 静态资源加载正常 | HTTP 200 | |
| 4 | `verify_deploy.sh` 全部通过 | PASS=X FAIL=0 | |
| 5 | 大表治理采集后显示数据 | 采集后表格显示大表列表（如有大表） | |
| 6 | 登录功能正常 | admin可登录 | |
| 7 | 服务开机自启 | `systemctl is-enabled` = enabled | |

---

## 六、重要注意事项

1. **不要修改 `.env` 文件**：v1.0.3 的配置文件完全兼容 v1.0.3.1
2. **不要操作数据库**：v1.0.3.1 无破坏性表结构变更，元数据库无需迁移
3. **保留备份**：`/opt/tdsql-sqlcheck-v1.0.3-backup` 不要删除，用于回滚
4. **大表治理测试**：如果测试数据库没有大表（>1GB），会显示"未发现大表"，这是正常现象。生产环境有大表时会正常显示。

---

## 七、部署完成后汇报

部署完成后，请汇报以下内容：

1. 每一步的执行结果（成功/失败）
2. `curl /health` 的完整输出
3. `verify_deploy.sh` 的完整输出
4. 如有任何报错，贴出完整错误信息
5. 大表治理模块采集后是否正常显示数据（如有大表）

---

## 八、联系与支持

- 部署问题：参考《部署手册-v1.0.2》主手册
- 功能使用：参考《功能使用手册》
- 运维操作：参考《运维手册》
- 代码仓库：GitHub smudy-linsang/tdsql-sqlcheck

---

**文档版本**: v1.0.3.1
**编写日期**: 2026-07-10
**编写人**: 编码智能体
