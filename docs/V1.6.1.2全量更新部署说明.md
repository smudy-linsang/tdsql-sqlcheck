# TDSQL-SQLCheck V1.6.1.2 全量更新与部署指南

| 项 | 内容 |
|---|---|
| **源版本** | V1.6.1.0 / V1.6.1.1（或 V1.0.x ~ V1.6.1.x 任意历史版本） |
| **目标版本** | **V1.6.1.2** |
| **更新方式** | **全量更新 (Full Release Package)** |
| **部署对象** | 内网运维智能体 / DBA 运维团队 |
| **适用环境** | 国产麒麟 V10 SP3 / RedHat / CentOS / Ubuntu，TDSQL 数据库集群 |

---

## 一、 版本核心变更与架构升级总览 (V1.6.1.0 ➔ V1.6.1.2)

从 V1.6.1.0 升级至 V1.6.1.2，系统完成了以下四大核心优化与交互体验治理：

1. **实例管理客户端三维筛选与换行治理 (V1.6.1.1)**：
   - 实例管理列表增加“连接名模糊搜索”、“主机地址”、“实例类型”三维筛选框与客户端高效分页；
   - 优化表格列宽（地址列 260px、操作列 480px + `nowrap` 防折行），统一 WARNING 警告表头视觉风格；
   - 将 `query_rewrite` 与 `xa` 系统数据库加入默认过滤黑名单。

2. **地址列 Tooltip 与探查异常防超时 (V1.6.1.2 - Commit 4c0a3c4)**：
   - 实例表格主机地址列增加 `show-overflow-tooltip` 提示（A-P3-03），避免长域名地址截断；
   - 混合失败场景下，只要包含任一 1045 权限错误即精准识别为 `NO_BUSINESS_USER` 并展示友情提示（A-P2-04）；
   - MonitorDB 连接失败增加单次尝试结论缓存，防止重复链接导致的超时累加与界面卡顿（A-P2-03）。

3. **系统元数据库（System Meta-Database）完全继承与自动迁移**：
   - 完整支持 v7（原始慢日志）、v8（ZK配置）、v9（连接唯一约束）、v10（扫描富集与留痕）连续数据库 Schema 结构。

4. **全量自动化测试套件通过 (1288 用例全过)**：
   - 新增 5 条多维度筛选与异常防卡顿专项测试，全量回归 1288 PASS / 0 FAIL / 0 SKIP。

---

## 二、 核心重点：元数据库（System Meta-Database）变更与自动迁移详解

**在升级至 V1.6.1.2 过程中，后台服务启动时会自动安全地应用全套增量 Schema 迁移文件（含 v7 原始慢日志、v8 ZK配置、v9 连接唯一约束、v10 扫描富集与留痕）以及 `migrator.run_migrations()`，无需运维人员手动执行 DDL 脚本。**

元数据库包含历史连续演进架构：
- **v7 架构**：5 张原始慢日志系统表（`slow_log_sources`, `slow_log_source_nodes`, `slow_log_cursors`, `slow_log_events`, `slow_log_runs`）；
- **v8 架构**：`zk_discovery_config` 表存储 ZK 节点配置、Proxy 映射规则、段替换与富集参数；
- **v9 架构**：`tdsql_connections` 增加 `(host, port, database)` 联合唯一索引 `uk_host_port_database`（修正去重判据，绝不按同名误删）；
- **v10 架构**：`zk_discovery_config` 增加扫描富集配置列（`octet_rules_json`, `enrich_enabled` 等），`zk_discovery_import_items` 增加 `name_source` / `databases_source` 留痕列。

---

## 三、 全量部署与安装操作步骤（内网运维智能体执行）

### 1. 解压全量发布包与部署预检
```bash
# 解压全量发布包
tar -zxvf tdsql-sqlcheck-v1.6.1.2-linux-x86_64.tar.gz
cd tdsql-sqlcheck-v1.6.1.2

# 执行部署预检（要求全 PASS，验证依赖与环境健康）
bash deploy/preflight_check.sh
```

### 2. 配置文件校验 (`deploy/.env`)
核对并更新 `deploy/.env` 配置文件中的元数据库与网络参数：
```ini
SQLCHECK_DB_HOST=127.0.0.1
SQLCHECK_DB_PORT=13306
SQLCHECK_DB_USER=root
SQLCHECK_DB_PASSWORD=tdsql_test_2024
SQLCHECK_DB_NAME=tdsql_sqlcheck
AUTH_ENABLED=true
ADMIN_INITIAL_PASSWORD=Admin@1234
```

### 3. 执行全量安装与服务升级重启
```bash
# 执行安装脚本（停止已有服务、覆盖全量代码、重新安装 wheels 依赖、升级数据库 Schema）
sudo ./deploy/install.sh
```
> ⚠️ **重要提醒**：`kazoo` 为本版必配离线依赖。全量安装时**必须完整运行 `./deploy/install.sh`（或执行 `pip install --no-index --find-links wheels/ -r requirements.txt`）**，切勿仅手动替换代码目录。

### 4. 上线最后自检 (必做步骤)
```bash
# 在目标部署机上运行部署校验脚本
bash deploy/verify_deploy.sh
```
`verify_deploy.sh` 会自动检测 Uvicorn 进程、端口监听、版本比对（输出 `1.6.1.2`）与 `/health` 端点，全部输出 PASS 即代表升级成功。

---

## 四、 内网真实环境测试验证指令

1. **服务健康验证**：
   ```bash
   curl -s http://127.0.0.1:8000/health
   # 预期输出: {"status":"ok","version":"1.6.1.2"}
   ```
2. **实例管理与 ZooKeeper 发现验证**：
   - 校验实例管理列表三维筛选、地址 Tooltip 及 ZK 探查异常提示。
